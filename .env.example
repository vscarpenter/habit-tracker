# AWS deployment configuration — copy to .env and fill in values.
# .env is gitignored; .env.example is tracked.

BUCKET_NAME=your-s3-bucket-name
DIST_ID=your-cloudfront-distribution-id
DOMAIN_NAME=your-domain.com

# Supabase sync (optional) — copy to .env.local and fill in values.
# Without these, the app works fully offline; sync features are disabled.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
