# AWS deployment configuration — copy to .env and fill in values.
# .env is gitignored; .env.example is tracked.

BUCKET_NAME=your-s3-bucket-name
DIST_ID=your-cloudfront-distribution-id
DOMAIN_NAME=your-domain.com

# PocketBase sync (optional) — copy to .env.local and fill in values.
# Without this, the app works fully offline; sync features are disabled.
NEXT_PUBLIC_POCKETBASE_URL=https://api.vinny.io

# PocketBase admin credentials (for one-time setup script).
# Example:
# bun run setup:pocketbase-sync
POCKETBASE_ADMIN_EMAIL=admin@example.com
POCKETBASE_ADMIN_PASSWORD=change-me
