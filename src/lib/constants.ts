/** User-facing error message for DB failures. */
export const DB_ERROR_MSG = "Something went wrong. Your data is safe.";

/** Auto-dismiss delay for toast notifications (ms). */
export const TOAST_AUTO_DISMISS_MS = 3000;

/** Duration of the completion bounce/fill animation (ms). */
export const COMPLETION_ANIMATION_MS = 200;

/** Delay before resetting toggle animation state (ms). */
export const TOGGLE_ANIMATION_RESET_MS = 400;

/** Delay for dialog close animation (ms). */
export const DIALOG_CLOSE_ANIMATION_MS = 200;

/** Minimum consecutive days to qualify as a "hot streak". */
export const HOT_STREAK_MIN_DAYS = 3;

/** Default emoji icon for new habits. */
export const DEFAULT_HABIT_ICON = "🏃";

/** Default target for x_per_week frequency. */
export const DEFAULT_X_PER_WEEK_TARGET = 3;
